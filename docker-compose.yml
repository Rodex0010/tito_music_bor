version: '3.8'
services:
  hasii:
    build: .
    container_name: hasii_music_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./tito/cookies:/app/tito/cookies
      - ./downloads:/app/downloads
    command: bash start
